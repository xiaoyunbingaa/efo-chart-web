# 任务：实现 packages/utils 基础工具

目标
- 在 packages/utils 中创建数据格式化和时间序列归一化示例函数，并在 index.ts 中导出。

步骤
1. 在 packages/utils 下创建 src/data/ 和 src/format/ 目录（或在现有位置添加文件）。
2. 添加 normalizeTimeSeries(rawData) 和 formatNumber(value) 的最小实现和注释。
3. 在 packages/utils/package.json 保持私有 workspace，运行类型检查或简单构建（如需）。
4. 记录执行日志到 logs/v1.0.0/run.log，并写 summary.md。

输出
- logs/v1.0.0/run.log
- logs/v1.0.0/summary.md
